[project]
name = "compressor"
version = "0.1.0"
description = "XOR representation learning experiments"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cma>=4.4.1",
    "matplotlib>=3.10.8",
    "pytest>=9.0.2",
    "pyyaml>=6.0.3",
    "torch>=2.9.1",
]
